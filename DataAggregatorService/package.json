{
  "name": "liqui-dot-data-aggregator",
  "version": "1.0.0",
  "description": "Standalone data aggregation service for LiquiDOT - fetches and stores Dune Analytics data in time series format",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "sync": "node scripts/manual-sync.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "dune-analytics",
    "data-aggregation",
    "time-series",
    "defi",
    "liquidity-pools",
    "polkadot"
  ],
  "author": "LiquiDOT Team",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^1.6.0",
    "node-cron": "^3.0.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 